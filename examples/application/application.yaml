apiVersion: application.azuread.jet.crossplane.io/v1alpha1
kind: Application
metadata:
  name: hello-crossplane
spec:
  forProvider:
    description: "Managed with Crossplane AzureAD Provider (generated with Terrajet)"
    visibility: public
    template:
      - owner: ajwgroup
        repository: provider-jet-azuread
  providerConfigRef:
    name: default
